server:
  port: 8001
spring:
  application:
    name: seata-order-provider
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: member
        group: SEATA_GROUP
      config:
        namespace: member
        group: SEATA_GROUP
        file-extension: yaml
